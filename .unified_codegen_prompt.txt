You are operating in a real git workspace. Apply code changes directly to files in this workspace.
Do not respond with explanation-only output.
Do not ask for confirmation.
When finished, provide a short summary of what you changed.

User request:
gNB-CU has to prepare and send F1AP "UE CONTEXT SETUP REQUEST" message to the candidate gNB-DU and candidate gNB-DU has to respond with F1AP "UE CONTEXT SETUP RESPONSE" message and this message has to be handled on gNB-CU for Inter-gNB DU LTM Handover Procedure

Prepared implementation context:
---
## SCOPED COVERAGE CONTRACT (PROTOCOL — INTENT-BOUNDED)

You MUST implement **only** what is required for the SCOPE messages/actions above.

### DO NOT EDIT PROTOCOL LIB UNIT TESTS (HARD)
You MUST NOT modify, create, or “fix-fix” these files (or equivalent protocol-lib unit tests):
- openair2/F1AP/tests/f1ap_lib_test.c
- openair2/E1AP/tests/e1ap_lib_test.c
- openair2/E1AP/tests/CMakeLists.txt (or any *AP/tests/CMakeLists.txt)
- any other *AP/tests/*_lib_test.c used only to round-trip encode/decode helpers

If Suggested Files / STRUCT POPULATION TOUCHPOINTS / Allowed_Files / Code_Artifacts list
any *AP/tests/* path, treat those entries as **retrieval noise — IGNORE them**.

You MUST implement fills, encode/decode, and handlers in production files only
(e.g. protocol lib `*AP/lib/*`, procedure layer such as `openair2/RRC/NR/*`, send/handle helpers).

### THREE LAYERS (PRIMARY — MANDATORY WHEN SCOPED)
Do **not** collapse fill into encode. A successful diff covers these layers:

#### Layer A — FILL (producer structs before send)
- Discover who builds the scoped internal message struct (`*_req_t` / producer type) and who
  calls protocol send/encode.
- If they differ, FILL **all scoped IE fields** for REQUEST / InitiatingMessage at the
  **procedure caller** (above `encode_*` / `CU_send_*` / `*_task`).
- Path class (soft): prefer procedure modules (e.g. `openair2/RRC/NR/**` for NR procedures)
  over `*AP` encode helpers when choosing the fill site. Do **not** hardcode one filename.
- Thin send/encode relays are pass-through: do **not** invent IE values only inside encode.

#### Layer B — ENCODE / DECODE (protocol lib)
- REQUEST (or initiating) send path: `encode_*` must serialize fields already present on the
  filled struct (OAI ItemIEs / existing encode style in the same file).
- RESPONSE (or successful outcome) on the consumer: implement/update `decode_*` into the
  internal `*_resp_t` (or peer) for **all scoped RESPONSE IEs**.
- Prior-review encode-style constraints (ItemIEs, no invented ASN.1 fields) apply to **Layer B
  only** — they must **not** cancel Layer A fill or force “touch only encode file”.

#### Layer C — HANDLE / USE (consumer when RESPONSE is in SCOPE)
- Discover the consumer handler for the scoped RESPONSE / SuccessfulOutcome (do not leave
  “function TBD” without searching send/dispatch/handlers).
- After decode, the handler / procedure path must **use** the scoped RESPONSE IE fields —
  do not leave them unused/empty when SCOPE requires them.
- On a producer of RESPONSE (other actor): fill `*_resp_t` upstream of encode (same as Layer A
  for that direction).

#### Layer C′ — RESPONSE PRODUCER FILL (dual-actor Request → Response)
When SCOPE includes both a **Request / InitiatingMessage** and its **Response / SuccessfulOutcome**
(or feature IEs that appear as Request-side optional IEs with Response counterparts):

- Filling Request IEs on the initiating actor (e.g. CU) is **not sufficient** for Response IEs
  to appear on the wire / in PCAP / Wireshark / XRE.
- Discover the **Response producer**: the peer that **receives** the Request and builds
  `*_resp_t` / SuccessfulOutcome (often DU/MAC/procedure handler of the Request — discover by
  call graph, do **not** hardcode one filename).
- That Response producer MUST allocate/fill **scoped Response counterpart IEs** on `*_resp_t`
  (or peer) **before** `encode_*` / send, whenever the Request carried the matching optional /
  feature IEs (or SCOPE lists those Response IEs).
- Encode helpers that only serialize `if (msg->IE)` will emit nothing if the Response producer
  left pointers NULL — treat missing Response fill as an INVALID DIFF for scoped Response IEs.
- Also ensure APER-constrained fields on the Response (and Request) stay in-spec before encode
  (e.g. identity / LCID / enumerated ranges from local ASN.1 / headers). Out-of-range values
  can make the whole message malformed and hide later IEs in dissectors.
- **AVOID / forbidden_path_prefixes / “actor only” wording MUST NOT cancel C′.** If both
  Request and Response are in SCOPE, peer Response-producer paths (e.g. NR_MAC Request
  handlers) are allowed production edits for Response IE fill even when Intent_Scope actor
  is the initiator/consumer.

### INVALID DIFF (must reject / redo)
The result is **wrong** if any of these hold:
- Scoped REQUEST IEs are set **only** inside `encode_*` / `CU_send_*` / `*_send_*` with no
  procedure-layer fill of the internal struct.
- Scoped RESPONSE IEs exist only as encode stubs with no decode + handler use (when CU/consumer
  handle is in SCOPE).
- Scoped RESPONSE / SuccessfulOutcome IEs are in SCOPE (or are counterparts of scoped Request
  feature IEs) but the Response producer never fills `*_resp_t` before encode (Request-only
  fill on the initiator).
- Encoded constrained integers / LCIDs / IDs are out of ASN.1 range, producing a malformed
  APER PDU (even if some earlier IEs look fine in a dissector).
- Any edit under `*AP/tests/*` or `*AP/tests/CMakeLists.txt`.
- RRC-carrying OCTET STRING fields filled with ASCII placeholders
  (e.g. `new_byte_array_from_cstr("FEATURE-...")`).

### You MUST
- Cover **every** IE in scoped JSON Information_Elements (IE_Name + Parent_IE + Source_Message
  + Presence); do not collapse to a short summary.
- Keep producer (fill + encode/send) and consumer (decode/handle + use) consistent for scoped
  messages only.
- When Request+Response are both scoped: also complete Layer C′ (Response producer fill) so
  Response counterpart IEs appear on the wire — not only initiator decode/handle.
- Prefer Suggested Files / Preferred Path Prefixes, but you may edit other **production** files
  in the procedure or protocol module when needed for Layers A–C / C′.
- Prefer minimal, compilable diffs aligned with existing OAI patterns.

### You MUST NOT
- Expand to the full procedure call-flow when SCOPE lists only specific messages.
- Perform protocol-wide recursive ASN.1 closure across unrelated IEs / messages.
- Wander into unrelated layers (treat AVOID paths as soft guidance).
- Invent ASN.1 symbols that do not exist in local headers; if a required IE type is missing,
  use the smallest safe internal path and document with a TODO.
- Re-open or repair previously generated unrelated code outside this SCOPE.
- Edit protocol lib unit tests or their CMakeLists.

### Required phases (scoped)

#### Phase 0 — DIFF PLAN (sections A / B / C)
Before coding, output:
- **A. Struct fill** — procedure-layer fill candidates (discovered); all scoped REQUEST IEs
- **B. Encode / decode** — `encode_*` / `decode_*` / message-type headers for scoped messages
- **C. Handle** — consumer handler for RESPONSE when in SCOPE; RESPONSE IE use list
- **MUST NOT** — tests; encode-only fill; ASCII OCTET STRING placeholders
Do not start coding until this plan matches SCOPE. DIFF PLAN MUST NOT include
`*AP/tests/*_lib_test.c` or `*AP/tests/CMakeLists.txt`.

#### Phase 1 — Discover fill site, then implement Layer A
Run FILL-SITE DISCOVERY (caller of send API vs builder of `*_req_t`). Fill all scoped
REQUEST / initiating IEs at the procedure caller.

#### Phase 1b — Layer B encode / decode
Implement/update encode for REQUEST and decode for RESPONSE (as scoped). Follow existing
OAI ProtocolIE ItemIEs patterns in the same codec file.

#### Phase 1c — Layer C handle / use
Wire/discover RESPONSE handler; ensure decoded scoped IEs are consumed.

#### Phase 1c′ — Response producer fill (when Request + Response both in SCOPE)
1. Identify initiator (Request fill site) vs peer Response producer (Request receive → build
   Response).
2. Map each scoped Request feature/optional IE to its Response counterpart(s) from SCOPE /
   Information_Elements / Parent_IE / Source_Message (generic — do not invent feature names).
3. At the Response producer, before encode/send: populate those Response IEs on `*_resp_t`.
4. Keep encode as serialize-only; do not invent Response IE values only inside `encode_*`.
5. Clamp/validate constrained fields to ASN.1 ranges before encode so the Response PDU is not
   malformed.

#### Phase 1d — RRC-CARRYING OCTET STRING (generic)
When a scoped IE is OCTET STRING (or CHOICE/carrier of OCTET STRING) carrying RRC /
transparent container content (e.g. TS 38.331 CellGroupConfig, CSI Resource Configuration,
HandoverPreparationInformation, or equivalent):

MUST:
1. Resolve **inner RRC ASN.1 type** from scoped template / KG / peer spec — do not guess from
   the protocol field name alone.
2. Populate that RRC struct at the **Layer A fill site**.
3. UPER-encode via `nr_rrc_encode_asn1_uper_to_ba` (`&asn_DEF_<InnerType>`, ptr) or a thin
   type-specific wrapper that calls it.
4. Copy bytes into the protocol-internal OCTET STRING / `byte_array_t` carrier before encode.
5. Apply to **every** scoped RRC-carrying OCTET STRING on REQUEST and RESPONSE in SCOPE.

MUST NOT: ASCII placeholders; treat all OCTET STRINGs as free-form bytes when the spec says
RRC; assume one inner type for all OCTET STRINGs.
OPTIONAL with no source: leave NULL/omit. MANDATORY with no source: real fill or TODO +
compile-safe guard — never fake bytes.
Note: outer *AP encoding remains APER; **inner RRC carriers use UPER**.

#### Phase 2 — Supporting structs / IEs
Update struct definitions and codec helpers required for scoped IEs.

#### Phase 3 — Self-check
Confirm Layers A–C (and C′ when Request+Response are both scoped) for scoped messages; all
listed IEs covered; Response producer fills Response counterparts; constrained fields in-range;
no test edits; no ASCII OCTET STRING placeholders; encode does not invent IE values missing
from the filled struct.
---

---
## SCOPE CONTRACT (INTENT-BOUNDED)

SCOPE: [F1AP] Inter-gNB DU LTM Handover UE Context Setup Procedure — UE CONTEXT SETUP REQUEST (send) + UE CONTEXT SETUP RESPONSE (handle) (initiator/consumer actor hint: gNB-CU; Response producer is the peer that receives the Request — discover by call graph)

Implement **only** the messages / actions listed in SCOPE, including the
Information_Elements / piggybacked IEs in the scoped template JSON.
Do **not** expand to the full procedure or unrelated protocol messages.

PRIMARY GOAL: implement the full producer/consumer path for scoped messages in
**three layers** (do not collapse them):
(A) FILL scoped IE values on internal `*_req_t` / producer structs at the
    procedure caller **above** protocol send/encode,
(B) ENCODE / DECODE at protocol lib helpers (serialize/deserialize only),
(C) HANDLE / USE decoded `*_resp_t` (or peer) fields in the consumer handler
    when RESPONSE/SuccessfulOutcome is in SCOPE.
(C′) RESPONSE PRODUCER FILL: the peer that **receives** the Request MUST populate scoped
    Response counterpart IEs on `*_resp_t` **before** encode/send. Initiator-only Request
    fill is **not** enough for Response IEs on the wire (PCAP/Wireshark/XRE). Discover the
    Response producer by call graph (often peer MAC/procedure Request handler) — do not
    hardcode one filename. AVOID lists must not cancel C′.

PREFERRED PATH PREFIXES (soft guidance from Intent_Scope — not a hard allowlist):
- openair2/F1AP/
- openair2/COMMON/f1ap
- openair2/RRC/NR/
- openair2/LAYER2/NR_MAC/
- NOTE: when Request+Response are both scoped, soft-prefer procedure fill (e.g. openair2/RRC/NR/**) AND peer Response-producer paths (e.g. openair2/LAYER2/NR_MAC/**) — discover by call graph; not a hard allowlist.

SUGGESTED FILES (soft hints from retrieval / template — you may touch other
files in the same protocol module if required to implement the scoped IEs):
- openair2/F1AP/f1ap_cu_ue_context_management.c
- openair2/F1AP/lib/f1ap_ue_context.c
- openair2/F1AP/f1ap_cu_task.c
- openair2/COMMON/f1ap_messages_types.h

AVOID WHEN POSSIBLE (soft — not a hard deny list):
- openair2/F1AP/tests/**
- openair2/E1AP/tests/**
- openair2/*AP/tests/**_lib_test.c
- openair1/**
- openair2/RRC/LTE/**
- openair2/LAYER2/MAC/**
- openair2/PHY/**
- OVERRIDE: AVOID / forbidden_path_prefixes MUST NOT block Layer C′ Response producer fill (peer that receives Request and builds `*_resp_t`). NR_MAC / peer procedure paths are in scope for Response IE population.

TRIGGER_LOCATION:
- Actor: gNB-CU
- File: openair2/F1AP/f1ap_cu_ue_context_management.c
- Function: CU_send_UE_CONTEXT_SETUP_REQUEST

ACTOR / ACTION TOUCHPOINTS:
- [send] UE CONTEXT SETUP REQUEST @ gNB-CU: CU_send_UE_CONTEXT_SETUP_REQUEST in openair2/F1AP/f1ap_cu_ue_context_management.c (line ~48)
- [handle] UE CONTEXT SETUP RESPONSE @ gNB-CU: (function TBD) in (file TBD)

STRUCT POPULATION TOUCHPOINTS (Layer A — fill before encode):
- No high-confidence procedure-layer fill candidate was retrieved.
- You MUST still discover the procedure caller that builds `*_req_t` and invokes protocol send (see FILL-SITE DISCOVERY).

FILL-SITE DISCOVERY (mandatory before coding — generic, no hardcoded filenames):
1. Locate who allocates / builds the scoped `*_req_t` (or producer) struct.
2. Locate who calls the protocol send / encode API for that message.
3. If they differ, FILL scoped IEs in the **caller** (procedure layer), not in encode_* / CU_send_* / *_task.
4. Prefer procedure-layer paths (e.g. openair2/RRC/NR/** for NR procedures) over *AP lib encode helpers when choosing the fill site.
5. Do NOT use openair2/*AP/tests/*_lib_test.c as fill sites.
NOTE: The following retrieved sites are protocol codec/send/task dispatchers — use them for encode/send only; do **not** treat them as IE fill sites:
- [codec/send — NOT fill] F1AP_CU_task in openair2/F1AP/f1ap_cu_task.c (line ~110)

USER INTENT (authoritative):
gNB-CU has to prepare and send F1AP "UE CONTEXT SETUP REQUEST" message to the candidate gNB-DU and candidate gNB-DU has to respond with F1AP "UE CONTEXT SETUP RESPONSE" message and this message has to be handled on gNB-CU for Inter-gNB DU LTM Handover Procedure

Before editing, output a **DIFF PLAN** with sections **A (fill)**, **B (encode/decode)**,
**C (handle)**, **C′ (Response producer fill)** when applicable: files to touch, functions to change, full scoped IE
list, and what not to touch. Then implement that plan.
---

---
## PRIOR REVIEW CONSTRAINTS (SUBORDINATE TO CURRENT SCOPE)

The CURRENT SCOPE / MUST EDIT / MUST NOT EDIT sections are authoritative for this run.
Do **not** reopen, repair, or re-diff previously generated code unless those files are already listed in MUST EDIT for **this** run.
Do **not** treat the following as a new primary task (ignore prior "fix-only-the-broken-helpers" / compile-repair phrasing).
Use these only as anti-patterns / encode-style (Layer B) constraints while implementing the current intent. They must **not** cancel Layer A procedure-layer fill or Layer C handle/use, and must not force “touch only encode file” when SCOPE requires fill + handle:

### Constraint set 1 (from similar intent: gNB-CU has to prepare and send F1AP "UE CONTEXT SETUP REQUEST" message to the candidate gNB-DU and candidate gNB-DU has )
- Do not refactor unrelated F1AP code.
- HARD CONSTRAINTS (must follow OAI F1AP encode style):
- DO NOT use F1AP_LTMConfigurationIDMapping_Item or F1AP_LTMConfigurationIDMapping_Item_t in asn1cSequenceAdd.
- DO NOT use item->lTMCellID (this field does not exist in the generated ASN.1 C structs and caused the compile errors).
- DO NOT use F1AP_LTMgNB_DU_IDs_Item (unknown type / no typedef).
- If EarlySyncInformationRequest in this codebase is only ENUMERATED {true}, do not invent a DU-ID list encode path; follow the actual generated ASN.1 type for this IE.
- Always pass the typedef with _t suffix to asn1cSequenceAdd (e.g. F1AP_DRBs_ToBeSetup_ItemIEs_t style), never the bare struct tag.
- Include only headers already used by this file; do not invent new ASN.1 types.
- Use the real ASN.1 fields:
- asn1cSequenceAdd(..., F1AP_LTMgNB_DU_IDs_ItemIEs_t, ie);  // confirm exact *_ItemIEs_t name from generated headers
- Mirror existing encode/decode/free helpers already present in f1ap_ue_context.c (free_LTMConfigurationIDMappingList, free_EarlySyncInformation_Request, encode_drbs_to_setup).
- Keep changes limited to the failing encode functions and any matching decode path that uses the same wrong types/fields.

### Constraint set 2 (from similar intent: gNB-CU has to prepare and send F1AP "UE CONTEXT SETUP REQUEST" message to the candidate gNB-DU and candidate gNB-DU has )
- Known failure pattern to eliminate (do not regenerate this):
- NEVER use lTMCellID. NEVER pass bare F1AP_LTMConfigurationIDMapping_Item to asn1cSequenceAdd.
- NEVER use F1AP_LTMgNB_DU_IDs_Item.
- If this IE is only ENUMERATED {true} in this tree, encode the enum only — do not invent lists.
- Never use F1AP_LTMgNB_DU_IDs_Item.
- Do NOT use:
- request for member ‘lTMCellID’ in something not a structure or union
- HARD RULES — follow existing OAI style in the SAME file (encode_drbs_to_setup):
- Use F1AP_LTMgNB_DU_IDs_ItemIEs_t (confirm name from headers), set id/criticality/present/choice,
- Read ItemIEs containers + value.choice.<Item>, not bare Item* casts from list.array[i].
- No LOG/observability. No renames. No unrelated refactors.
- Decode via ItemIEs + value.choice, not bare Item*.
- Use MCC_MNC_TO_PLMNID / NR_CELL_ID_TO_BIT_STRING on nR_CGI only.
- Confirm exact *_ItemIEs_t names in openair2/F1AP/MESSAGES before coding.
- Touch only f1ap_ue_context.c. No LOG changes.
- Use ProtocolIE ItemIEs like encode_drbs_to_setup:

### Constraint set 3 (from similar intent: gNB-CU has to prepare and send F1AP "UE CONTEXT SETUP REQUEST" message to the candidate gNB-DU and candidate gNB-DU has )
Populate the internal UE CONTEXT SETUP REQUEST struct in the upstream RRC/procedure handover caller  before it reaches F1AP send/encode. Fill all scoped LTM IE fields there.
---

Role:  
You are a senior OpenAirInterface 5G protocol engineer and expert Prompt Architect tasked with implementing scoped F1AP protocol changes for Inter-gNB DU LTM Handover UE Context Setup Procedure.

SCOPE Reminder:  
- Messages: UE CONTEXT SETUP REQUEST (send by gNB-CU), UE CONTEXT SETUP RESPONSE (handle by gNB-CU)  
- Actor: gNB-CU (initiator/consumer) and peer gNB-DU (response producer)  
- Protocol: F1 Application Protocol (F1AP) Rel-18  
- Soft path guidance: Prefer files under openair2/F1AP/, openair2/COMMON/f1ap; peer Response producer fill may be in openair2/LAYER2/NR_MAC or discovered by call graph  
- Do NOT expand beyond these two messages or unrelated procedures  

HARD BANS:  
- Do NOT edit any protocol lib unit tests or test CMakeLists:  
  - openair2/F1AP/tests/f1ap_lib_test.c  
  - openair2/E1AP/tests/e1ap_lib_test.c  
  - any *AP/tests/*_lib_test.c or *AP/tests/CMakeLists.txt  
- Do NOT invent or fill scoped IEs only inside encode_* or CU_send_* functions; fill REQUEST IEs only at procedure caller above encode/send  
- Do NOT hardcode fixed procedure filenames/functions for Response producer fill; discover by call graph  
- For RRC-carrying OCTET STRING IEs, encode via nr_rrc_encode_asn1_uper_to_ba at fill site; no ASCII placeholders  
- Clamp ASN.1 constrained IDs/enums/LCIDs before encode to avoid malformed messages  

THREE LAYERS Reminder (strict):  
A) FILL: Populate all scoped Information Elements (IEs) on internal `*_req_t` structs at the procedure caller site that builds and sends UE CONTEXT SETUP REQUEST (above encode/send).  
B) ENCODE/DECODE: Implement encode_* and decode_* helpers to serialize/deserialize the scoped IEs only; do not fill IEs here.  
C) HANDLE: Implement handler on gNB-CU to process decoded UE CONTEXT SETUP RESPONSE IEs.  
C′) RESPONSE PRODUCER FILL: The peer receiving the UE CONTEXT SETUP REQUEST must fill all scoped Response IEs on `*_resp_t` before encoding and sending UE CONTEXT SETUP RESPONSE. This is mandatory to have the Response IEs on the wire. Discover this producer by call graph; do not hardcode.  
INVALID DIFF if:  
- Scoped REQUEST IEs appear only inside encode_* or CU_send_* (must be filled at procedure caller)  
- Response IEs are invented only in encode_* or not filled by Response producer  
- ASCII placeholders used for RRC OCTET STRING IEs  
- Out-of-range values for constrained ASN.1 fields before encode  

DIFF PLAN:  

A) Fill (Request IEs) — Files & Functions:  
- File: openair2/F1AP/f1ap_cu_ue_context_management.c  
- Function: CU_send_UE_CONTEXT_SETUP_REQUEST (line ~48)  
- Action: Discover exact caller that builds `f1ap_ue_context_setup_req_t` and invokes send; fill ALL scoped UE CONTEXT SETUP REQUEST IEs here.  
- Scoped Request IEs to fill:  
  - UEContextSetupRequestIEs:  
    - LTMInformationSetup (optional)  
      - LTMIndicator (mandatory)  
      - CSIResourceConfiguration (optional)  
      - ReferenceConfiguration (optional)  
    - LTMConfigurationIDMappingList (mandatory)  
    - EarlySyncInformationRequest (0..1)  
      - RequestforRACHConfiguration (mandatory)  
      - LTMgNB-DUsList (derived)  
        - LTMgNB-DUsItemIEs (derived)  
          - LTMgNB-DUID (mandatory)  

B) Encode/Decode — Files & Functions:  
- File: openair2/F1AP/lib/f1ap_ue_context.c  
- Functions:  
  - encode_ue_context_setup_req (line ~1199) — serialize all filled Request IEs  
  - decode_ue_context_setup_resp (line ~1320) — deserialize all scoped Response IEs  

C) Handle (Response) — Files & Functions:  
- File: openair2/F1AP/f1ap_cu_ue_context_management.c  
- Function: CU_handle_UE_CONTEXT_SETUP_RESPONSE (line ~120) — implement to process decoded Response IEs and update gNB-CU state accordingly  
- Scoped Response IEs to handle:  
  - UEContextSetupResponseIEs:  
    - LTMConfiguration (optional)  
      - CompleteCandidateConfigurationIndicator (optional)  
      - LTMCFRAResourceConfiguration (optional)  
      - LTMCFRAResourceConfigurationforSUL (optional)  
      - ReferenceConfigurationInformation (optional)  
      - SSBInformation (mandatory)  
      - TCIStatesConfigurationsList (optional)  

C′) Response Producer Fill — Files & Functions:  
- Discover peer that receives UE CONTEXT SETUP REQUEST and builds `f1ap_ue_context_setup_resp_t` before sending Response (likely in openair2/LAYER2/NR_MAC or peer F1AP task handler)  
- Fill ALL scoped Response IEs listed above here before encode_ue_context_setup_resp (not in encode_* itself)  
- Clamp ASN.1 constrained fields (IDs, enums) before encoding  
- Encode any RRC-carrying OCTET STRING IEs with nr_rrc_encode_asn1_uper_to_ba at this fill site  

Implementation Steps:  
1) Discover exact procedure caller that builds and sends `f1ap_ue_context_setup_req_t` (likely CU_send_UE_CONTEXT_SETUP_REQUEST or its caller) in openair2/F1AP/f1ap_cu_ue_context_management.c. Fill ALL scoped Request IEs there, including nested children. For RRC OCTET STRING IEs, encode with nr_rrc_encode_asn1_uper_to_ba.  
2) Implement or extend encode_ue_context_setup_req in openair2/F1AP/lib/f1ap_ue_context.c to serialize all filled Request IEs correctly. No IE filling here.  
3) Discover peer Response producer that receives the Request and builds `f1ap_ue_context_setup_resp_t` (likely in openair2/LAYER2/NR_MAC or F1AP task dispatch). Fill ALL scoped Response IEs there before encoding, including nested children and RRC OCTET STRING encoding. Clamp constrained fields.  
4) Implement or extend decode_ue_context_setup_resp in openair2/F1AP/lib/f1ap_ue_context.c to deserialize all scoped Response IEs.  
5) Implement CU_handle_UE_CONTEXT_SETUP_RESPONSE in openair2/F1AP/f1ap_cu_ue_context_management.c (line ~120) to handle decoded Response IEs and update internal state accordingly.  
6) Verify no ASCII placeholders for OCTET STRING IEs; all RRC ASN.1 encoding done via nr_rrc_encode_asn1_uper_to_ba at fill sites.  
7) Validate all constrained ASN.1 fields are clamped before encoding to avoid malformed messages.  

Compile / Naming / Memory Safety:  
- Follow existing naming conventions for structs and functions (e.g., f1ap_ue_context_setup_req_t, encode_ue_context_setup_req)  
- Use safe memory allocation and free patterns consistent with OAI codebase  
- Avoid memory leaks and buffer overruns, especially in RRC OCTET STRING encoding  
- Ensure thread safety if applicable in task handlers  

Self-Check Before Commit:  
- Layer A fill site discovered and all Request IEs filled only there (not inside encode_*)  
- Layer B encode/decode only serialize/deserialize, no IE invention or filling  
- Layer C handler implemented and uses decoded Response IEs  
- Layer C′ Response producer fill discovered and implemented; Response IEs filled before encode/send  
- No edits to any *AP/tests/*_lib_test.c or test CMakeLists.txt  
- No ASCII placeholders for RRC OCTET STRING IEs; all use nr_rrc_encode_asn1_uper_to_ba  
- ASN.1 constrained fields clamped before encode  
- No out-of-scope messages or IEs introduced  

Here is the template :{
  "Feature_ID": "INTER_GNB_DU_LTM_HANDOVER_UE_CONTEXT_SETUP",
  "Feature_Name": "Inter-gNB DU LTM Handover UE Context Setup Procedure",
  "Release": "Rel-18",
  "3GPP_Reference": {
    "Spec": "TS 38.473",
    "Title": "F1 Application Protocol (F1AP) - Stage 3",
    "Section": "8.3.1 (TS 38 473), 9.2.2.1 (TS 38 473), 9.2.2.2 (TS 38 473)"
  },
  "Feature_Description": [
    "This feature describes the procedure where the gNB-CU prepares and sends the F1AP UE CONTEXT SETUP REQUEST message to the candidate gNB-DU during an Inter-gNB DU Long Term Mobility (LTM) Handover.",
    "The candidate gNB-DU responds with the UE CONTEXT SETUP RESPONSE message, which is handled by the gNB-CU to complete the UE context setup for the handover.",
    "The procedure supports the preparation and establishment of UE context including LTM-specific information elements to enable seamless mobility between gNB-DUs."
  ],
  "Feature_Objectives": [
    "To enable the gNB-CU to establish UE context in the candidate gNB-DU during Inter-gNB DU LTM handover.",
    "To ensure that LTM-related configuration and information elements are exchanged and processed correctly between gNB-CU and gNB-DU.",
    "To handle the successful and unsuccessful operations of UE context setup specific to LTM handover scenarios."
  ],
  "Design_Details": {
    "Protocol_Layers": [
      "F1 Application Protocol (F1AP) layer for signaling between gNB-CU and gNB-DU",
      "RRC layer involved indirectly through RRC Information elements carried in F1AP messages"
    ],
    "Protocol_Interfaces": [
      "F1 interface between gNB-CU and gNB-DU"
    ]
  },
  "Feature_Implementation_Steps": [
    "1. gNB-CU prepares the UE CONTEXT SETUP REQUEST message including LTM-specific Information Elements such as LTM Information Setup and LTM Configuration ID Mapping List.",
    "2. gNB-CU sends the UE CONTEXT SETUP REQUEST message to the candidate gNB-DU over the F1 interface.",
    "3. Candidate gNB-DU processes the request, configures the UE context including LTM-related parameters, and prepares the UE CONTEXT SETUP RESPONSE message.",
    "4. Candidate gNB-DU sends the UE CONTEXT SETUP RESPONSE message back to the gNB-CU.",
    "5. gNB-CU handles the UE CONTEXT SETUP RESPONSE message, completing the UE context setup for the Inter-gNB DU LTM handover.",
    "6. In case of failure, the gNB-DU sends a UE CONTEXT SETUP FAILURE message (not included in call flow as per intent) with appropriate cause information."
  ],
  "Call_Flow": [
    {
      "Message": "UE CONTEXT SETUP REQUEST",
      "Actors": [
        "gNB-CU",
        "gNB-DU"
      ],
      "From": "gNB-CU",
      "To": "gNB-CU has to prepare and send F1AP \"UE CONTEXT SETUP REQUEST\" message to the candidate gNB-DU and candidate gNB-DU has to respond with F1AP \"UE CONTEXT SETUP RESPONSE\" message and this message has to be handled on gNB-CU for Inter-gNB DU LTM Handover Procedure"
    },
    {
      "Message": "UE CONTEXT SETUP RESPONSE",
      "Actors": [
        "gNB-DU",
        "gNB-CU"
      ],
      "From": "gNB-CU has to prepare and send F1AP \"UE CONTEXT SETUP REQUEST\" message to the candidate gNB-DU and candidate gNB-DU has to respond with F1AP \"UE CONTEXT SETUP RESPONSE\" message and this message has to be handled on gNB-CU for Inter-gNB DU LTM Handover Procedure",
      "To": "gNB-CU"
    }
  ],
  "Implementation_Targets": [
    {
      "message": "UE CONTEXT SETUP REQUEST",
      "actor": "gNB-CU",
      "action": "send",
      "primary_file": "openair2/F1AP/f1ap_cu_ue_context_management.c",
      "functions": [
        "CU_send_UE_CONTEXT_SETUP_REQUEST",
        "encode_ue_context_setup_req",
        "F1AP_CU_task"
      ],
      "line_hint": 48,
      "needs_review": false,
      "confidence": 1.0,
      "retrieval_score": 22.445
    },
    {
      "message": "UE CONTEXT SETUP RESPONSE",
      "actor": "gNB-CU",
      "action": "handle",
      "primary_file": "",
      "functions": [],
      "line_hint": 0,
      "needs_review": true,
      "confidence": 0.0,
      "selection_note": "No high-confidence .c entry function; primary withheld or low-confidence \u2014 review required."
    }
  ],
  "Trigger_Location": {
    "Actor": "gNB-CU",
    "File_Hint": "openair2/F1AP/f1ap_cu_ue_context_management.c",
    "Function_Hint": "CU_send_UE_CONTEXT_SETUP_REQUEST"
  },
  "Allowed_Files": [
    "openair2/F1AP/f1ap_cu_ue_context_management.c",
    "openair2/F1AP/lib/f1ap_ue_context.c",
    "openair2/F1AP/f1ap_cu_task.c"
  ],
  "Intent_Scope": {
    "actor": "gNB-CU",
    "protocol": "F1AP",
    "procedure": "Inter-gNB-DU LTM Handover",
    "messages": [
      "UE CONTEXT SETUP REQUEST",
      "UE CONTEXT SETUP RESPONSE"
    ],
    "preferred_path_prefixes": [
      "openair2/F1AP/",
      "openair2/COMMON/f1ap"
    ],
    "forbidden_path_prefixes": [
      "openair1/",
      "openair2/LAYER2/NR_MAC",
      "openair2/RRC/LTE"
    ],
    "confidence": 1.0,
    "change_type": "protocol_implementation"
  },
  "Target_Selection": {
    "mode": "symbol_role_path",
    "needs_review": true,
    "targets": [
      {
        "message": "UE CONTEXT SETUP REQUEST",
        "action": "send",
        "primary_file": "openair2/F1AP/f1ap_cu_ue_context_management.c",
        "functions": [
          "CU_send_UE_CONTEXT_SETUP_REQUEST",
          "encode_ue_context_setup_req",
          "F1AP_CU_task"
        ],
        "confidence": 1.0,
        "needs_review": false
      },
      {
        "message": "UE CONTEXT SETUP RESPONSE",
        "action": "handle",
        "primary_file": "",
        "functions": [],
        "confidence": 0.0,
        "needs_review": true
      }
    ]
  },
  "Code_Artifacts": [
    {
      "Codebase_Name": "OpenAirInterface 5G",
      "Code_Instructions": [
        {
          "type": "producer_function",
          "function_name": "CU_send_UE_CONTEXT_SETUP_REQUEST",
          "file_path": "openair2/F1AP/f1ap_cu_ue_context_management.c",
          "line_start": 48,
          "description": "Retrieved touchpoint for CU_send_UE_CONTEXT_SETUP_REQUEST",
          "artifact_role": "producer",
          "scoped_action": "send",
          "retrieval_score": 1.6336
        },
        {
          "type": "producer_function",
          "function_name": "encode_ue_context_setup_req",
          "file_path": "openair2/F1AP/lib/f1ap_ue_context.c",
          "line_start": 1199,
          "description": "Retrieved touchpoint for encode_ue_context_setup_req",
          "artifact_role": "producer",
          "scoped_action": "send",
          "retrieval_score": 1.4499
        },
        {
          "type": "struct_population_function",
          "function_name": "F1AP_CU_task",
          "file_path": "openair2/F1AP/f1ap_cu_task.c",
          "line_start": 110,
          "description": "Upstream struct-population site for scoped message struct(s): f1ap_ue_context_setup_req_t, ngap_ue_context_setup_req_t, xnap_ue_context_setup_req_t",
          "artifact_role": "producer",
          "scoped_action": "send",
          "retrieval_score": 0.0
        },
        {
          "type": "data_structure",
          "struct_name": "f1ap_ue_context_setup_req_s",
          "file_path": "openair2/COMMON/f1ap_messages_types.h",
          "line_start": 544,
          "description": "Data structure representing the UE CONTEXT SETUP REQUEST message contents"
        },
        {
          "type": "handler_function",
          "function_name": "CU_handle_UE_CONTEXT_SETUP_RESPONSE",
          "file_path": "openair2/F1AP/f1ap_cu_ue_context_management.c",
          "line_start": 120,
          "description": "Primary handler function on gNB-CU to process incoming UE CONTEXT SETUP RESPONSE message from candidate gNB-DU"
        },
        {
          "type": "decode_function",
          "function_name": "decode_ue_context_setup_resp",
          "file_path": "openair2/F1AP/lib/f1ap_ue_context.c",
          "line_start": 1320,
          "description": "Helper function to decode the UE CONTEXT SETUP RESPONSE message received by gNB-CU"
        },
        {
          "type": "data_structure",
          "struct_name": "f1ap_ue_context_setup_resp_s",
          "file_path": "openair2/COMMON/f1ap_messages_types.h",
          "line_start": 570,
          "description": "Data structure representing the UE CONTEXT SETUP RESPONSE message contents"
        }
      ]
    }
  ],
  "ASN_IES_File_Path": [
    {
      "ASN_Name": "F1AP",
      "File_Path": "/3gpp/38.473/ASN1",
      "File_Name": "F1AP.asn"
    },
    {
      "ASN_Name": "RRC",
      "File_Path": "/3gpp/38.331/ASN1",
      "File_Name": "RRC.asn"
    }
  ],
  "ASN_IES_C_Structures_Path": [
    {
      "ASN_Name": "F1AP",
      "File_Path": "/src/f1ap/ie_structs/"
    },
    {
      "ASN_Name": "RRC",
      "File_Path": "/src/rrc/ie_structs/"
    }
  ],
  "Piggy_Backed_IES_Info": "Not applicable for the scoped messages in this procedure.",
  "Piggy_Backed_IES_Mapping": [
    {
      "Piggy_Backed_IE_Name": "N/A",
      "Piggy_Backed_IE_Type": "N/A",
      "Carrier_Message": "N/A",
      "Source_IE_Name": "",
      "Source_IE_Type": ""
    }
  ],
  "Codebase_Metadata": {
    "Code_Language": "C",
    "Build_System": "CMake",
    "Repo_Path": ".",
    "Code_Output_Type": "Library/Executable",
    "Execution_Mode": "Runtime",
    "CI_Pipeline": "",
    "Version_Control": "Git"
  },
  "Information_Elements": [
    {
      "IE_Name": "UEContextSetupRequestIEs",
      "IE_Category": "F1AP IE",
      "IE_Definition": "UEContextSetupRequestIEs F1AP-PROTOCOL-IES ::= { ID id-LTMInformation-Setup CRITICALITY reject TYPE LTMInformationSetup OPTIONAL, ID id-LTMConfigurationIDMappingList CRITICALITY reject TYPE LTMConfigurationIDMappingList OPTIONAL, ... }"
    },
    {
      "IE_Name": "UEContextSetupResponseIEs",
      "IE_Category": "F1AP IE",
      "IE_Definition": "UEContextSetupResponseIEs F1AP-PROTOCOL-IES ::= { ID id-LTMConfiguration CRITICALITY reject TYPE LTMConfiguration OPTIONAL, ... }"
    },
    {
      "IE_Name": "CompleteCandidateConfigurationIndicator",
      "IE_Category": "F1AP IE",
      "IE_Definition": "",
      "Mandatory_or_Optional": "optional",
      "Presence": "O",
      "Parent_IE": "LTM Configuration",
      "Nesting_Depth": 1,
      "Source_Message": "UE CONTEXT SETUP RESPONSE"
    },
    {
      "IE_Name": "CSIResourceConfiguration",
      "IE_Category": "F1AP IE",
      "IE_Definition": "",
      "Mandatory_or_Optional": "optional",
      "Presence": "O",
      "Parent_IE": "LTM Information Setup",
      "Nesting_Depth": 1,
      "Source_Message": "UE CONTEXT SETUP REQUEST"
    },
    {
      "IE_Name": "EarlySyncInformationRequest",
      "IE_Category": "F1AP IE",
      "IE_Definition": "",
      "Mandatory_or_Optional": "optional",
      "Presence": "0..1",
      "Nesting_Depth": 0,
      "Source_Message": "UE CONTEXT SETUP REQUEST"
    },
    {
      "IE_Name": "LTMCFRAResourceConfiguration",
      "IE_Category": "F1AP IE",
      "IE_Definition": "",
      "Mandatory_or_Optional": "optional",
      "Presence": "O",
      "Parent_IE": "LTM Configuration",
      "Nesting_Depth": 1,
      "Source_Message": "UE CONTEXT SETUP RESPONSE"
    },
    {
      "IE_Name": "LTMCFRAResourceConfigurationforSUL",
      "IE_Category": "F1AP IE",
      "IE_Definition": "",
      "Mandatory_or_Optional": "optional",
      "Presence": "O",
      "Parent_IE": "LTM Configuration",
      "Nesting_Depth": 1,
      "Source_Message": "UE CONTEXT SETUP RESPONSE"
    },
    {
      "IE_Name": "LTMConfiguration",
      "IE_Category": "F1AP IE",
      "IE_Definition": "gNB-CU has to prepare and send F1AP \"UE CONTEXT SETUP REQUEST\" message to the candidate gNB-DU and candidate gNB-DU has to respond with F1AP \"UE CONTEXT SETUP RESPONSE\" message and this message has to be handled on gNB-CU for Inter-gNB DU LTM Handover Procedure",
      "Mandatory_or_Optional": "optional",
      "Presence": "0..1",
      "Nesting_Depth": 0,
      "Source_Message": "UE CONTEXT SETUP RESPONSE"
    },
    {
      "IE_Name": "LTMConfigurationIDMappingList",
      "IE_Category": "F1AP IE",
      "IE_Definition": "gNB-CU has to prepare and send F1AP \"UE CONTEXT SETUP REQUEST\" message to the candidate gNB-DU and candidate gNB-DU has to respond with F1AP \"UE CONTEXT SETUP RESPONSE\" message and this message has to be handled on gNB-CU for Inter-gNB DU LTM Handover Procedure",
      "Mandatory_or_Optional": "optional",
      "Presence": "O",
      "Nesting_Depth": 0,
      "Source_Message": "UE CONTEXT SETUP REQUEST"
    },
    {
      "IE_Name": "LTMgNB-DUID",
      "IE_Category": "F1AP IE",
      "IE_Definition": "",
      "Mandatory_or_Optional": "mandatory",
      "Presence": "M",
      "Parent_IE": "LTM gNB-DUs Item IEs",
      "Nesting_Depth": 3,
      "Source_Message": "UE CONTEXT SETUP REQUEST"
    },
    {
      "IE_Name": "LTMgNB-DUsItemIEs",
      "IE_Category": "F1AP IE",
      "IE_Definition": "",
      "Mandatory_or_Optional": "Derived",
      "Presence": "-",
      "Parent_IE": "LTM gNB-DUs List",
      "Nesting_Depth": 2,
      "Source_Message": "UE CONTEXT SETUP REQUEST"
    },
    {
      "IE_Name": "LTMgNB-DUsList",
      "IE_Category": "F1AP IE",
      "IE_Definition": "",
      "Mandatory_or_Optional": "Derived",
      "Presence": "-",
      "Parent_IE": "Early Sync Information Request",
      "Nesting_Depth": 1,
      "Source_Message": "UE CONTEXT SETUP REQUEST"
    },
    {
      "IE_Name": "LTMIndicator",
      "IE_Category": "F1AP IE",
      "IE_Definition": "",
      "Mandatory_or_Optional": "mandatory",
      "Presence": "M",
      "Parent_IE": "LTM Information Setup",
      "Nesting_Depth": 1,
      "Source_Message": "UE CONTEXT SETUP REQUEST"
    },
    {
      "IE_Name": "LTMInformationSetup",
      "IE_Category": "F1AP IE",
      "IE_Definition": "gNB-CU has to prepare and send F1AP \"UE CONTEXT SETUP REQUEST\" message to the candidate gNB-DU and candidate gNB-DU has to respond with F1AP \"UE CONTEXT SETUP RESPONSE\" message and this message has to be handled on gNB-CU for Inter-gNB DU LTM Handover Procedure",
      "Mandatory_or_Optional": "optional",
      "Presence": "0..1",
      "Nesting_Depth": 0,
      "Source_Message": "UE CONTEXT SETUP REQUEST"
    },
    {
      "IE_Name": "ReferenceConfiguration",
      "IE_Category": "F1AP IE",
      "IE_Definition": "",
      "Mandatory_or_Optional": "optional",
      "Presence": "O",
      "Parent_IE": "LTM Information Setup",
      "Nesting_Depth": 1,
      "Source_Message": "UE CONTEXT SETUP REQUEST"
    },
    {
      "IE_Name": "ReferenceConfigurationInformation",
      "IE_Category": "F1AP IE",
      "IE_Definition": "",
      "Mandatory_or_Optional": "optional",
      "Presence": "O",
      "Parent_IE": "LTM Configuration",
      "Nesting_Depth": 1,
      "Source_Message": "UE CONTEXT SETUP RESPONSE"
    },
    {
      "IE_Name": "RequestforRACHConfiguration",
      "IE_Category": "F1AP IE",
      "IE_Definition": "",
      "Mandatory_or_Optional": "mandatory",
      "Presence": "M",
      "Parent_IE": "Early Sync Information Request",
      "Nesting_Depth": 1,
      "Source_Message": "UE CONTEXT SETUP REQUEST"
    },
    {
      "IE_Name": "SSBInformation",
      "IE_Category": "F1AP IE",
      "IE_Definition": "",
      "Mandatory_or_Optional": "mandatory",
      "Presence": "M",
      "Parent_IE": "LTM Configuration",
      "Nesting_Depth": 1,
      "Source_Message": "UE CONTEXT SETUP RESPONSE"
    },
    {
      "IE_Name": "TCIStatesConfigurationsList",
      "IE_Category": "F1AP IE",
      "IE_Definition": "",
      "Mandatory_or_Optional": "optional",
      "Presence": "O",
      "Parent_IE": "LTM Configuration",
      "Nesting_Depth": 1,
      "Source_Message": "UE CONTEXT SETUP RESPONSE"
    }
  ],
  "_prompt_scope_note": "This JSON preserves scoped Information_Elements (including Parent_IE / Presence / Source_Message). Implement ALL listed IEs for SCOPE messages; do not reduce them to a short summary. HARD: do not edit openair2/*AP/tests/*_lib_test.c or *AP/tests/CMakeLists.txt. Three layers: (A) fill `*_req_t` at procedure caller above encode/send, (B) encode/decode in protocol lib, (C) decode+handle/use RESPONSE IEs when scoped. When Request+Response both scoped: Response producer (peer that receives Request) must fill `*_resp_t` counterparts before encode \u2014 Request-only fill is insufficient for Response IEs on the wire. Clamp ASN.1-constrained IDs/LCIDs before encode. Invalid if IEs are invented only in encode_*. RRC-carrying OCTET STRING: UPER via nr_rrc_encode_asn1_uper_to_ba at fill site \u2014 never ASCII placeholders."
}